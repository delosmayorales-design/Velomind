{
  "name": "velomind-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Envoltorio Capacitor de VeloMind para Android nativo (grabacion GPS en segundo plano). No es una app independiente: empaqueta cyclocoach/ tal cual.",
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "sync": "npm run build:www && npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
